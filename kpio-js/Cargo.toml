[package]
name = "kpio-js"
version = "0.1.0"
edition = "2021"
authors = ["KPIO Team"]
description = "JavaScript engine for KPIO OS"
license = "MIT OR Apache-2.0"

[dependencies]
libm = "0.2"

[features]
default = []
std = []
